<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.app.hungerdelivery">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HungerDelivery"
        android:usesCleartextTraffic="true">
        <activity android:name=".Cardapio"
            android:parentActivityName=".CategoriaPizzaria"></activity>
        <activity android:name=".CategoriaPizzaria"
            android:parentActivityName=".CategoriaComercios"/>
        <activity
            android:name=".ConfiguracaoUsuario"
            android:parentActivityName=".Home" />
        <activity android:name=".Home" />
        <activity
            android:name=".ProdutoEmpresa"
            android:parentActivityName=".PainelEmpresa" />
        <activity
            android:name=".ConfiguracaoEmpresa"
            android:parentActivityName=".PainelEmpresa" />
        <activity android:name=".MainActivity" />
        <activity android:name=".PainelEmpresa" />
        <activity android:name=".CategoriaComercios" />
        <activity android:name=".CadastroUsuario" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>